// Logical operators

// && -> AND
// || -> OR
// !  -> Negation

let isLoggedIn = true;
let isPaid = true;

console.log(isLoggedIn && isPaid);

let isEmailUser = true;
let googleUser = false;

console.log(isEmailUser || googleUser);
